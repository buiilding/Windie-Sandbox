name: check

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  release-notes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate release notes
        run: scripts/check-release-notes.sh

  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - run: cargo fmt --check
      - run: cargo test
      - run: cargo clippy --all-targets -- -D warnings

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vendor/windie-inspector/frontend
    steps:
      - uses: actions/checkout@v4
      - name: Checkout Inspector submodule
        working-directory: .
        run: git submodule update --init --depth 1 vendor/windie-inspector
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: vendor/windie-inspector/frontend/package-lock.json
      - run: npm ci --legacy-peer-deps
      - run: npm run build
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
          targets: x86_64-pc-windows-msvc
      - run: cargo fmt --check
      - run: cargo check --target x86_64-pc-windows-msvc
      - run: cargo test --target x86_64-pc-windows-msvc
