# Browser interactions

Use a browser-specific capability when Windie provides one for the requested
browser task. Use the CUA Driver when the task genuinely requires visible
desktop interaction that the browser capability cannot provide.

Before submitting, sending, purchasing, deleting, or changing account
settings, confirm that the user explicitly requested that consequential action.
