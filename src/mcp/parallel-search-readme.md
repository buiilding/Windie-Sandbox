# Parallel Search

Parallel Search lets Windie search the live web and extract content from URLs through a hosted MCP provider.

## Requirements

Parallel supports anonymous basic usage. Add a `PARALLEL_API_KEY` for higher rate limits.

## Safety

This provider uses the network and sends requests to an external service. Review network-enabled tools before attaching them to a conversation.
