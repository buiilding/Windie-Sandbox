# Parallel Search

Parallel Search lets Windie search the live web and extract content from URLs
through a hosted MCP provider.

Parallel supports anonymous basic usage. Add `PARALLEL_API_KEY` for higher
rate limits.
